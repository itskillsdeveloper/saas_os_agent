[project]
name = "saas_os_agent"
authors = [{ name = "SaaS OS", email = "ops@example.com"}]
description = "In-tenant governance agent for SaaS OS: enforces plan quotas the control plane cannot reach from outside the site."
requires-python = ">=3.14"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
